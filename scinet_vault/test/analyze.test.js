import assert from 'assert'

function fakeServer() {}

describe('Summarize API basic contract (placeholder)', () => {
  it('should have placeholder tests (to be run with node test runner)', () => {
    assert.ok(true)
  })
})
